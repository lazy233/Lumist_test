# 本地开发：复制为 .env 后按需修改
# DATABASE_URL 必填（PostgreSQL），格式：postgresql://用户名:密码@主机:端口/数据库名
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@localhost:5432/todo_db

# 自然语言创建任务：阿里云百炼（DashScope）API Key，与 OPENAI_API_KEY 二选一即可
# BAILIAN_API_KEY=sk-xxx
# OPENAI_API_KEY=sk-xxx

# 可选：阿里模型与地址（默认 qwen-turbo）
# ALI_MODEL=qwen-turbo
# ALI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
