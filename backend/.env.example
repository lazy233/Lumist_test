# 复制为 .env 后，根目录执行 docker compose up -d --build 即可（数据库由 compose 启动）
# Docker 下使用下面这行即可，无需改
DATABASE_URL=postgresql://todo_user:123456@db:5432/todo_db

LOG_LEVEL=INFO

# 自然语言创建任务（可选）
# BAILIAN_API_KEY=sk-xxx
# OPENAI_API_KEY=sk-xxx
